spring:
  application:
    name: hrm
  datasource:
    url: jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}
    username: ${PGUSER}
    password: ${PGPASSWORD}
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true
springdoc:
  api-docs:
    enabled: true
    path: /api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
server: 
  forward-headers-strategy: framework
jwt:
    secret-key: "oPpOqZ0CzA7jE2B8f9Yqk3sV7jMq2PjW9JdRzQxv6tHk4eMb9fDqYzNs1gR5uX3L"
    access-expiration: 1800000        # 30 phút
    refresh-expiration: 604800000     # 7 ngày
company:
    email-domain: "congty.abc.com"
