spring:
  application:
    name: hrm
  datasource:
    url: jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}
    username: ${PGUSER}
    password: ${PGPASSWORD}
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true
springdoc:
  api-docs:
    enabled: true
    path: /api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
server: 
  forward-headers-strategy: framework
jwt:
    secret-key: "3f8c1e9b6a24d7f0c92e4b71a3fd58c1e6ad47b29ac3f410b9c83db41ff29a3e"
    expiration: 1800000
company:
    email-domain: "congty.abc.com"
